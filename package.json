{
    "scripts": {
      "postman-test": "newman run test.json -e qa.json -r allure --reporter-allure-export allure-results",
      "allure-generate": "allure generate allure-results --clean -o allure-report"
    }
  }
  